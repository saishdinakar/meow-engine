# Playful Yes/No Site

A tiny static practice site with a playful yes/no prompt and a "No" button that loves to dodge.

## Local preview

From the project root, run:

```bash
python -m http.server 8000
```

Then open <http://localhost:8000> in your browser.

## Free hosting (no custom domain required)

- **GitHub Pages**: push to a GitHub repo and enable Pages in **Settings → Pages** (deploy from `main` / root). You'll get a `github.io` URL.
- **Netlify Drop**: drag and drop the folder into Netlify for an instant `netlify.app` URL.
- **Cloudflare Pages**: connect the repo and get a `pages.dev` URL with automatic deploys.
